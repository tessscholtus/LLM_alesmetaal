# --- Core PDF & parsing ---
pdfplumber==0.11.4      # PDF -> tekst/lay-out
pypdf==5.0.1            # Alleen tekst/structuur (snel, geen lay-out)

# --- LLM / API ---
openai==1.51.2          # Voor llm_extractor.py

# --- Data ---
pandas==2.2.2
openpyxl==3.1.5         # Excel lees/schrijf via pandas

# --- Config & util ---
python-dotenv==1.0.1    # API keys via .env
pydantic==2.8.2         # Datavalidatie van velden
tqdm==4.66.4            # Progress bars
loguru==0.7.2           # Mooie logging
